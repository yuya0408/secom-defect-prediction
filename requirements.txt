# 推論 API と学習スクリプトの実行に必要なパッケージ。
# モデル（models/*.pkl）を生成した環境とバージョンを揃えることで、
# joblib.load 時のバージョン不整合による警告・不具合を防ぐ。

# --- 推論・学習のコア ---
scikit-learn==1.8.0
pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
joblib==1.5.3

# --- API サーバー ---
fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4

# --- ノートブックを再実行する場合のみ必要（API 実行には不要） ---
matplotlib==3.10.8
